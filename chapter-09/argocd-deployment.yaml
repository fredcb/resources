apiVersion: v1
kind: Deployment
metadata:
  name: argocd-server
spec:
  template:
    spec:
      containers:
      - command:
        - argocd-server
        - "--insecure"
        - "--staticassets"
        - "/shared/app"
        name: argocd-server
